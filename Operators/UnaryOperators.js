/***
 *  Unary operators
 *         -> these are those operators which require only one operand
 *
 *       a++
 *       Postfix unary addition
 *
 *       ++a
 *       Prefix unary addition
 *
 *       a--;
 *       --a;
 *
 *
 *      +, - , !, typeof()
 *
 *
 *
 */

// let a = 10;
// let y = a++;
// a++;
// console.log(a, y);

// let a = 10;
// let y = ++a;
// ++a;
// console.log(a, y);

/**
 * 
//Unary Plus
//   +

// it tries to converti the variable into a number if it is not already a no.
// +x it doesn't change the original operand but instead returns a converted value

// let x = "22";
// let y = +x;

// console.log(typeof x);
// console.log(typeof y);


 */

/**
 * Unary Minus
 *      -
 *
 *  -> it also converts the operand to a number, but always make the result after negaty it.
 *
 */

let x = "22";
let y = -x;
console.log(typeof y);
console.log(y);
