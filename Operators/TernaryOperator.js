/**
 * Ternary Operators
 *      ?:
 *
 * syntax:
 *
 *      let y = ( (condition) ? (exp1) : (exp2));
 */

let y = 10 > 5 ? 10 : 5;
console.log(y);

let z = 1 > 5 ? 10 : 6;
console.log(z);

let a = true ? 2 + 3 : 2 - 3;
console.log(a);
